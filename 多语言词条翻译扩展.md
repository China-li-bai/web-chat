# 多语言词条翻译扩展

## Core Features

- 新增 word_translations 表（wordId/userId/lang/text）

- ImportWord 支持 translations 数组

- ensureImportFileSchema 校验 translations

- 导入服务同步写入翻译数据

- AI 生成结构化输出附加 translations（可选）

## Tech Stack

{
  "Web": {
    "arch": "react",
    "component": "antd"
  }
}

## Design

保留 words 表结构，翻译独立存储；语言码使用 BCP47；前端按界面语言优先显示翻译，无则回退 definition。

## Plan

Note: 

- [ ] is holding
- [/] is doing
- [X] is done

---

[X] 确认多语言方案细节（语言码、默认中文、展示策略）

[X] DB 迁移：新增 word_translations 表及索引约束

[X] 类型更新：ImportTranslation、ImportWord.translations

[X] 服务更新：importWordbook/seed 同步写入 translations

[X] AI 生成：prompt 支持 translations，结构化返回

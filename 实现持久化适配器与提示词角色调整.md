# 实现持久化适配器与提示词角色调整

## Core Features

- saveGeneratedPractice(json,userId) 一键落库

- normalizeDialogueRoles() 安全映射非标准角色

- 提示词模板改为 user/assistant 并保留 originalRole

## Tech Stack

{
  "Web": {
    "arch": "node",
    "component": "null"
  },
  "DB": "SQLite (wa-sqlite adapter)"
}

## Design

持久化在一个独立的 service 中实现，插入 session/turn/messages，并将 tips/vocabulary 作为 system 消息存储；提示词输出结构与数据库角色对齐，减少持久化层复杂度。

## Plan

Note: 

- [ ] is holding
- [/] is doing
- [X] is done

---

[X] 归类与梳理

[X] 提示词-数据库对齐方案说明

[X] 实现持久化适配器（saveGeneratedPractice）

[X] 可选：调整提示词角色到标准集

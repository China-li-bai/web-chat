我将先完善奖励类型与规则的“可执行定义”，确保数据统一、可解释、可落地，然后再进行绑定实现。

奖励类型与规则（最小可执行版）

类型层级
连击徽章（Streak Badges）
Bronze: 连续 ≥3 天
Silver: 连续 ≥7 天
Gold: 连续 ≥14 天
Diamond: 连续 ≥30 天
当日目标徽章（Daily Goal）
Daily Focus: 当日完成 ≥ 配额（Σ 学习响应数 ≥ dailyQuota）
Overachiever: 当日完成 ≥ 1.5 × 配额
周度稳定（Weekly Consistency）
7 日内完成 ≥5 天 Daily Focus
掌握里程碑（Mastery Milestone）
某词书掌握率 ≥80% 且总词数 ≥100
挑战任务（Challenges，后续拓展）
连续 3 天保持正确率 ≥85%
数据来源与统一性
连击：基于 study_logs 的“每日有学习响应事件”连贯统计；以用户时区日界为准
当日目标：基于 TodayPlan.dailyQuota 与当日 study_logs 的响应计数（again/hard/good/easy 均计入）
周度稳定：近 7 天的 Daily Focus 达成计数
掌握里程碑：statsService 的词书掌握率聚合
规则细则
去重：同一类型同一等级仅授予一次；提升等级可叠加显示为最高级
冷却：当日目标每日最多记一次；Overachiever取代Daily Focus（不重复）
触发时机：在会话完成或“更新词书进度”时评估一次（后台/前台均可）
记录格式
Reward { id, type, level, title, desc, icon, grantedAt }
RewardGrantEvent { userId, date, type, level, meta }
服务与接口（LLM/代码可执行 Prompt）
evaluateRewardsOnEvent({ userId, eventType: 'sessionCompleted'|'progressUpdated', date }): Promise<{ granted: Reward[] }>
getUserRewards({ userId }): Promise<Reward[]>
computeStreak({ userId }): Promise
computeDailyGoal({ userId, date, dailyQuota }): Promise<{ achieved: boolean; overachiever: boolean }>
UI 接入（今日计划页与会话总结）
今日计划页 Hero：显示 streak 徽章（最高等级）、今日目标达成提示
会话总结 Modal：新增“今日奖励”区块，列表展示当次新授予的奖励
请确认以上奖励类型与规则。如果同意，我将：

在 services/rewardService.ts 实现 evaluateRewardsOnEvent/computeStreak/computeDailyGoal
在 LearningSessionPage 完成后调用评估并写入奖励
在 TodayPlan 页与总结 Modal 展示新奖励


我将实现奖励服务并在两个触发点评估奖励：

新增 src/services/rewardService.ts，定义奖励类型与规则评估：evaluateRewardsOnEvent、computeStreak、computeDailyGoal、getUserRewards
在 LearningSessionPage 会话完成时调用 evaluateRewardsOnEvent('sessionCompleted')，弹出提示新授予的奖励数量
在 TodayPlan 页“更新词书进度”按钮成功后调用 evaluateRewardsOnEvent('progressUpdated')，并简要提示新奖励
说明：为最小可执行落地，奖励的存储采用 localStorage，后续可替换为后端持久化；streak 初期读取 store/localStorage，后续绑定 study_logs 真实计算。

现在并行写入奖励服务并精准增量接入两个页面触发点。